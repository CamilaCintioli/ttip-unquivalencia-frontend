import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    margin: '0 auto',
    width: '100%',
    textAlign: 'center',
  },
}));

export default useStyles;
